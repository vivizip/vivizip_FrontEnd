import React, { Component } from "react";
import { Text, View } from "react-native";

export default class NonMemberButton extends Component {
  render() {
    return (
      <View>
        <Text> non-Member Button </Text>
      </View>
    );
  }
}
